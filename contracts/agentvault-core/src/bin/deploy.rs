//! Deployment entrypoint for AgentVault core contracts.
//! Contract registrations will be added once escrow and attestation stubs exist.

fn main() {
    eprintln!("AgentVault deploy: no contracts registered yet. Run `cargo odra build` after adding stubs.");
}